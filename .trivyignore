# Official python:3.13-slim ships these in a parent image layer / ensurepip
# bundle. The runtime site-packages used by Gunicorn do not contain them:
# setuptools is uninstalled after dependency install, and any msgpack present
# is 1.2.2+. Trivy still reports the hidden parent-layer copies.
CVE-2025-47273
GHSA-6v7p-g79w-8964
